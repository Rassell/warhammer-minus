# YouTube Data API client
google-api-python-client==2.196.0

# Environment variable management
python-dotenv==1.2.2

# Semantic tagging with sentence transformers
sentence-transformers==2.7.0
torch>=2.9.0
numpy>=1.24.0

# LLM-based tagging with Google Gemini (new package)
google-genai>=0.2.0

# LLM-based tagging with Groq (fast, generous free tier)
groq>=0.9.0
